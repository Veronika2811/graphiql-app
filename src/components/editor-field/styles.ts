import styled from '@emotion/styled';

export const EditorFieldStyle = styled('textarea')({
  resize: 'none',
  width: '100%',
  height: '100%',
  background: '#2B2929',
  color: 'white',
  fontSize: '1rem',
  maxHeight: '100%',
  minHeight: 150,
});
