import { KitSxProps } from 'type/interface';

const styles: KitSxProps = {
  box: {
    position: 'relative',
    textAlign: 'center',
    mb: 2,
  },
  spinner: {
    position: 'absolute',
  },
};

export default styles;
